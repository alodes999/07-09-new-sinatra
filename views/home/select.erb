<h1>Pick Product to <%= @menu_name %></h1>

<!--This form allows for input to select a product.  It returns the product id back to the controller to process the next form in our controller.  It directs where it needs to go from a variable set in the controller.  Sends a call to the "/product_change_input" receiver -->
  <form action="/<%= @menu_name.downcase %>_product_input">
    <select name="product[id]">
      <% @product.each do |product| %>
        <option value="<%= product.id %>"><%= product.name %></option>
      <% end %>
    </select>
    
    <button type="submit">Pick product to <%= @menu_name.downcase %></button>

  </form>
<p><a href="/home">Go to home page</a></p>